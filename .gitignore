.DS_Store
ref/
plan/
public/

# CV PDF is mirrored from the My_CV repo at build time (single source of truth).
static/cv/Full_CV_Resonance.pdf
# Sentinel regenerated by build.sh each run — gitignoring means a fresh clone
# renders the safe fallback link rather than an iframe pointing at a missing PDF.
data/cv_meta.toml
